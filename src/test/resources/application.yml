spring:
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 모드로 실행 ( 홈페이지에 있음 ), JVM내에서 실행되어 h2 데이터베이스가 꺼져있어도 실행 가능
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL : debug
#    org.hibernate.type : trace

# 싹다 주석해도 스프링부트는 따로 정보가 없으면 메모리 모드로 바꿔버림
